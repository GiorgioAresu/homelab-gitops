---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: fission
  namespace: fission
spec:
  chart:
    spec:
      chart: fission-all
      version: v1.19.0
      sourceRef:
        kind: HelmRepository
        name: fission
        namespace: flux-system
  interval: 5m0s
