---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kube-better-node
  namespace: kube-system
spec:
  chart:
    spec:
      # renovate: registryUrl=https://decayofmind.github.io/charts/
      chart: kube-better-node
      sourceRef:
        kind: HelmRepository
        name: decayofmind
        namespace: flux-system
      version: 0.0.4
  interval: 5m0s
